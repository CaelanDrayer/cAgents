# Executor config for OPERATE super-domain
# Inherits shared monitoring patterns from base config
_base: shared/config/base_executor_config.yaml

super_domain: operate
description: Execution monitoring for OPERATE super-domain

domain_specific_monitoring:
- Financial calculation accuracy verification
- Process documentation completeness
- Compliance checkpoint validation
