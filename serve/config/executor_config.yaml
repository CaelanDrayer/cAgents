# Executor config for SERVE super-domain
# Inherits shared monitoring patterns from base config
_base: shared/config/base_executor_config.yaml

super_domain: serve
description: Execution monitoring for SERVE super-domain

domain_specific_monitoring:
- SLA compliance tracking
- Legal review milestone completion
- Customer satisfaction impact monitoring
