# Executor config for GROW super-domain
# Inherits shared monitoring patterns from base config
_base: shared/config/base_executor_config.yaml

super_domain: grow
description: Execution monitoring for GROW super-domain

domain_specific_monitoring:
- Campaign asset creation progress
- Content review and approval status
- Sales enablement material completion
