{
  "$comment": "DEPRECATED - cAgents V9.5 requires Node.js. Shell-only mode is no longer supported. This file is kept for reference only. Use settings.json (CJS-only hooks).",
  "displayOrigin": "cAgents",
  "trustProjectMdFiles": true,
  "teammateMode": "tmux",
  "env": {
    "CAGENTS_VERSION": "9.5.0"
  },
  "permissions": {
    "allow": [
      "Read(**)",
      "Glob(**)",
      "Grep(**)",
      "Bash(git *)",
      "Write(Agent_Memory/**)"
    ],
    "deny": [
      "Bash(rm -rf /)",
      "Bash(rm -rf ~)",
      "Bash(sudo *)",
      "Write(/etc/**)",
      "Write(~/.ssh/**)"
    ]
  },
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "prompt",
            "prompt": "You are using cAgents V9.5. Node.js is required for hook functionality. Install Node.js and use the full settings.json configuration."
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Before stopping, verify: all work items completed with evidence, coordination_log.yaml written, outputs produced."
          }
        ]
      }
    ]
  }
}
